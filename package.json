{
  "name": "a-better-taiko-hiroba",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "bun": ">=1.2.19"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p packages/core",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "check:corpus": "bun run packages/core/scripts/check-corpus.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@types/bun": "^1.2.19",
    "typescript": "^5.6.0"
  }
}
